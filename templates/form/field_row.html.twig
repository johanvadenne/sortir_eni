{# Template personnalisé pour les champs de formulaire #}
<div class="mb-3">
    {{ form_label(form, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
    {{ form_widget(form, {'attr': {'class': 'form-control'}}) }}
    {{ form_help(form) }}
    {{ form_errors(form) }}
</div>
